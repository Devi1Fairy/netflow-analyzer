# 技术选型与架构决策记录

本文档记录Netflow Analyzer项目的重要技术、开发环境、测试方案和硬件选型。它回答的不只是“用了什么”，还包括“为什么使用、放弃了什么、承担了什么代价、何时需要重新评估”。

最后更新：2026-08-25。

## 1. 记录规则

每项决策使用以下状态：

| 状态 | 含义 |
|---|---|
| 已采用 | 已经进入正式项目并有代码或工程配置 |
| 基础完成 | 第一版已经工作，后续仍会扩展或替换 |
| 计划采用 | 方向已经确定，但尚未进入正式运行链路 |
| 暂缓 | 当前阶段收益不足，保留到满足条件后再做 |
| 待验证 | 已经选择，但需要硬件、性能测试或真实环境确认 |

项目版本、功能变化记录在`CHANGELOG.md`；实际故障及修复过程记录在`docs/problem_log.md`。本文档只记录技术决策，不把正常开发过程写成故障。

## 2. 决策总览

| 编号 | 决策 | 状态 |
|---|---|---|
| TD-001 | 核心程序使用Linux C11 | 已采用 |
| TD-002 | x86_64 Ubuntu作为开发环境，ARM64 Linux作为目标环境 | 已采用／待验证 |
| TD-003 | 使用CMake、Ninja和CTest组织构建与测试 | 已采用 |
| TD-004 | 使用libpcap，先完成离线PCAP再接实时抓包 | 已采用 |
| TD-005 | 使用边界检查游标和`memcpy`处理二进制数据 | 已采用 |
| TD-006 | 隔离第三方库类型，内部使用项目自己的数据模型 | 已采用 |
| TD-007 | 按Ethernet、IPv4和传输层拆分协议解析器 | 已采用 |
| TD-008 | 使用规范化双向五元组表示网络流 | 已采用 |
| TD-009 | 先用定长线性流表验证模型，再演进为哈希流表 | 基础完成 |
| TD-010 | 流键使用FNV-1a 64位非加密哈希 | 已采用 |
| TD-011 | 流记录按最后活动时间过期 | 基础完成 |
| TD-012 | C测试模块，Python测试完整命令行链路 | 已采用 |
| TD-013 | 单线程链路稳定后再接入pthread流水线 | 计划采用 |
| TD-014 | 先结构化输出，再做Qt和可选云端展示 | 计划采用 |
| TD-015 | 核心阶段不引入数据库 | 暂缓 |
| TD-016 | 异常检测先规则后机器学习 | 计划采用 |
| TD-017 | 目标开发板选择LubanCat-2N | 待验证 |
| TD-018 | 首次上板先原生编译，再评估交叉编译 | 计划采用 |
| TD-019 | 使用Git功能分支、Pull Request和版本标签 | 已采用 |

## 3. 核心语言与运行平台

### TD-001：核心程序使用Linux C11

状态：已采用。

背景：

项目面向嵌入式Linux应用开发、网络协议解析和DPI相关岗位，需要直接处理原始字节、网络字节序、固定宽度整数、内存生命周期、系统调用和多线程同步。

决定：

核心采集、解析、流聚合和规则检测使用ISO C11，并通过POSIX接口使用Linux能力。

主要理由：

- C能够直接表达字节布局、指针、缓冲区边界和资源生命周期；
- C11提供`stdint.h`、复合字面量、布尔类型和更明确的语言基础；
- libpcap和pthread都提供成熟的C接口；
- C程序运行时依赖较小，适合资源受限的ARM Linux设备；
- 该选择能直接展示嵌入式Linux岗位关注的底层能力。

没有把C++作为核心语言的原因：

C++可以提供RAII、容器和泛型，但第一阶段更需要明确练习内存、错误码和资源所有权。未来Qt上位机仍会使用C++，但不会要求采集和解析核心依赖Qt运行环境。

代价与复审条件：

- C缺少标准容器和自动资源管理，需要自行设计队列、哈希表和清理路径；
- 如果应用层解析器数量显著增加，或者模块扩展成本明显高于收益，可以评估在稳定C接口之外增加C++封装。

### TD-002：开发环境与目标环境分离

状态：开发环境已采用，目标环境待上板验证。

决定：

- 开发主机：x86_64 Ubuntu 24.04 LTS；
- 源码放在Ubuntu原生ext4目录；
- Windows上的VS Code通过Remote-SSH操作Ubuntu；
- 编译、测试、Git和抓包实际都在Ubuntu中执行；
- 最终目标：ARM64 Linux开发板。

主要理由：

- PC上的编译、调试和自动化测试速度更快；
- Linux环境与最终目标系统共享GCC、CMake、POSIX和libpcap生态；
- 先在PC排除业务逻辑错误，再定位ARM架构、驱动和性能差异；
- Ubuntu LTS的软件包、文档和维护周期适合持续开发。

为什么不从第一天只在开发板上写代码：

板端编译速度、存储、调试界面和故障恢复能力都弱于PC。只在板端开发会把协议逻辑问题与硬件环境问题混在一起，降低排查效率。

参考：[Ubuntu发布周期](https://ubuntu.com/about/release-cycle)。

## 4. 构建、测试与工程组织

### TD-003：使用CMake、Ninja和CTest

状态：已采用。

决定：

- CMake描述目标、源码、公开头文件、第三方依赖和编译选项；
- Ninja作为当前开发机的默认构建后端，Make仍可使用；
- Debug和Release使用不同构建目录；
- CTest统一启动C测试程序和Python端到端脚本；
- 导出`compile_commands.json`供VS Code和静态分析工具使用。

主要理由：

- 项目已经包含核心库、命令行程序和多个测试目标，手写单一gcc命令难以维护；
- CMake能够查找libpcap、Python和线程库，并为未来交叉编译提供工具链文件入口；
- Ninja增量构建输出简洁、速度快，但它只是后端，不成为源码的硬依赖；
- CTest按照退出码统一判断不同语言编写的测试是否通过。

为什么不长期维护手写Makefile：

Makefile仍需要理解，但当前项目的重点不是重复维护平台相关的依赖和链接规则。CMake生成Makefile或Ninja规则，能够减少构建配置重复。

参考：[CMake Testing and CTest](https://cmake.org/cmake/help/latest/guide/tutorial/Testing%20and%20CTest.html)。

### TD-012：C单元测试与Python端到端测试分工

状态：已采用。

决定：

- C测试直接调用模块接口，验证边界、错误码、状态和数据结构；
- Python脚本生成确定性PCAP，启动真实命令行程序并检查整体输出；
- CTest负责统一注册和执行。

主要理由：

- C测试离被测函数最近，失败后容易定位具体模块；
- Python更适合创建临时目录、拼装测试文件、启动子进程和匹配输出；
- Python不进入正式程序运行链路，因此不会增加板端部署依赖；
- 端到端测试弥补“所有模块单测通过，但应用连接方式错误”的盲区。

代价：

测试环境比正式运行环境多一个Python依赖。发布构建可以使用`BUILD_TESTING=OFF`关闭测试目标。

## 5. 数据采集与PCAP

### TD-004：使用libpcap，并从离线PCAP开始

状态：已采用。

决定：

正式程序通过libpcap读取原始数据包。第一阶段使用离线PCAP，后续实时抓包仍继续使用libpcap接口。

选择libpcap的原因：

- 能直接获得原始数据、时间戳、捕获长度和线路长度；
- 离线和实时抓包使用相近的数据模型，后续迁移成本较低；
- Linux网络分析工具生态成熟，便于与tcpdump和Wireshark交叉验证；
- 不需要自行处理不同平台的底层抓包设备接口。

为什么先做离线PCAP：

- 输入固定，可重复运行；
- 不依赖root权限、网卡状态、实时流量和抓包时机；
- 单元测试与端到端测试可以构造边界报文；
- 同一个文件可以交给本程序、tcpdump和Wireshark对比；
- 协议解析错误与实时采集错误能够分开定位。

为什么不直接解析tcpdump文本：

tcpdump适合人工排障，但文本格式不是稳定的业务API。启动子进程并解析文本会引入管道、进程生命周期、格式变化和错误传递问题。项目运行路径使用libpcap，tcpdump负责抓包和对照，Wireshark负责图形化逐字段验证。

为什么不只使用Wireshark：

Wireshark是成熟分析工具，但项目目标是开发可嵌入业务程序的采集、解析、聚合和异常检测模块，而不是只完成人工查看。自己基于libpcap实现可以继续接入线程、规则、告警和硬件部署。

代价与复审条件：

- libpcap是核心第三方依赖，目标系统必须提供对应头文件和库；
- 高速实时抓包可能出现丢包，接口速率不等于程序可持续处理速率；
- 如果常规libpcap无法满足真实吞吐量，再通过性能数据评估AF_PACKET、TPACKET_V3、AF_XDP或DPDK，而不是提前增加复杂度。

参考：[libpcap项目](https://github.com/the-tcpdump-group/libpcap)、[pcap_open_offline手册](https://www.tcpdump.org/manpages/pcap_open_offline.3pcap.html)。

## 6. 二进制数据与协议解析

### TD-005：边界检查游标、切片和`memcpy`

状态：已采用。

决定：

所有协议字段读取都以`caplen`为边界，通过字节游标执行读取、跳过和切片；原始字节复制使用`memcpy`，不使用字符串复制函数。

主要理由：

- 网络报文是二进制数据，内部允许出现`0x00`，没有字符串终止符；
- `strcpy`、`strlen`等接口无法表达报文长度和截断边界；
- 游标集中维护当前位置和剩余长度，减少每个解析器重复写指针运算；
- `memcpy`避免直接把未对齐地址强制转换为整数指针造成未对齐访问或别名问题；
- 显式的大端读取保证x86与ARM得到相同数值。

代价：

代码比直接强制转换更长，但换取了边界清晰、跨平台和可测试性。

### TD-006：隔离第三方库类型

状态：已采用。

决定：

只有`capture`模块直接包含libpcap头文件。上层使用`capture_t`、`capture_packet_t`和`CAPTURE_LINK_TYPE_ETHERNET`等项目类型，不直接依赖`pcap_t`或`DLT_EN10MB`。

主要理由：

- 限制第三方库影响范围；
- app和协议解析器不需要理解libpcap内部类型；
- 单元测试可以构造项目数据，不必伪造libpcap句柄；
- 未来替换或增加实时采集后端时，上层接口保持稳定。

这里的转换不是重复定义协议，而是在第三方边界上建立项目自己的抽象。

### TD-007：分层协议解析器

状态：已采用。

决定：

Ethernet、IPv4、TCP、UDP和ICMP分别由独立模块解析，通过负载视图逐层传递，不把所有字段读取写进`app.c`。

主要理由：

- 协议天然具有封装层次；
- 每个模块可以独立测试截断、格式错误和不支持状态；
- app只负责调度，不依赖每个字段偏移；
- 后续加入VLAN、IPv6或应用层协议时不会持续扩大单个函数。

## 7. 流量模型、流表与哈希

### TD-008：规范化双向五元组

状态：已采用。

决定：

使用源IPv4、源端口、目标IPv4、目标端口和协议号标识一条流，并对两个端点排序，形成`endpoint_a`和`endpoint_b`。

主要理由：

- 正向包和反向包应聚合到同一条连接记录；
- 规范化后只需要一个流表键；
- 两个方向的包数和字节数仍分别统计，不会丢失方向信息；
- ICMP没有TCP或UDP端口，当前阶段统一使用0端口。

限制：

标准五元组不能区分所有ICMP会话，也不处理NAT前后关联、隧道和IPv6。应用需求扩大后需要扩展键模型。

### TD-009：先用线性流表验证模型，再切换开放寻址哈希流表

状态：已采用。

决定：

第一版使用调用者提供的定长连续数组和线性查找；验证流键、方向、记录生命周期和输出正确后，切换为固定容量开放寻址哈希流表。哈希冲突使用线性探测解决，槽位使用`EMPTY`、`OCCUPIED`和`DELETED`三种状态。

为什么不一开始就写哈希表：

- 线性数组容易理解和调试；
- 第一版最多256条流，足够验证业务链；
- 先确定`flow_key_equal`和流记录模型，再处理冲突、负载因子、扩容和删除；
- 当结果错误时，可以先排除复杂的槽位状态。

为什么最终需要哈希：

线性查找平均和最坏复杂度都是`O(n)`；流数量增长后，每个数据包都扫描大量记录。哈希表通过`hash % capacity`定位候选槽位，平均查找接近`O(1)`。

为什么当前仍然固定容量：

- 固定容量可以明确内存上限，符合嵌入式程序对资源可预测性的要求；
- 调用者提供槽位数组，流表不需要决定使用栈、静态区还是堆；
- 当前容量为256，现阶段重点是验证冲突、删除和业务链正确性；
- 后续先测量流数量、负载因子和探测长度，再决定采用重建、动态扩容还是保持固定上限。

代价：

哈希表必须正确处理冲突、数组回绕、空槽和删除标记。删除后的槽位不能立即变成`EMPTY`，否则会中断其他冲突记录的查找；命中候选槽位后仍必须使用`flow_key_equal`确认完整流键。当前版本尚未实现负载因子控制和动态扩容，容量耗尽时返回`ENOSPC`。

### TD-010：使用FNV-1a 64位哈希

状态：已采用并接入正式流表定位。

决定：

对规范化流键的13个业务字节使用FNV-1a 64位算法。IPv4地址和端口按固定大端字节序参与计算，不直接哈希结构体内存。

选择FNV-1a的原因：

- 实现短小，只需要逐字节异或和乘法；
- 不需要动态内存、查表或第三方库；
- 适合长度固定且很短的五元组键；
- 64位结果提供足够大的哈希空间；
- 算法确定，便于使用固定测试向量验证x86和ARM一致性；
- RFC 9923将FNV描述为快速、分布良好的非加密哈希，并给出FNV-1a流程和参考实现。

为什么不用结构体原始内存：

`flow_key_t`可能含有编译器填充字节，且多字节整数在不同CPU上的内存字节序可能不同。逐字段按固定字节序处理可以得到跨平台稳定结果。

为什么没有选择其他算法：

- 简单累加或djb2容易实现，但字段混合和正式参考不足；
- MurmurHash和xxHash具有更强的吞吐性能，但实现和依赖更复杂，当前13字节键没有证明需要它们；
- SipHash能够抵抗攻击者构造大量冲突，但需要随机密钥且计算成本更高。

安全边界与复审条件：

FNV-1a不是加密算法，也不抵抗恶意碰撞。如果程序以后直接暴露在不可信高速网络中，并观察到冲突攻击或哈希退化，应改用带随机种子的SipHash等算法。无论使用哪种哈希，槽位命中后都必须调用`flow_key_equal`确认。

参考：[RFC 9923：The FNV Non-Cryptographic Hash Algorithm](https://datatracker.ietf.org/doc/html/rfc9923)。

### TD-011：按最后活动时间清理过期流

状态：基础完成。

决定：

使用`last_seen`和截止时间判断空闲流是否过期。哈希流表把过期槽位标记为`DELETED`，后续插入可以复用，但查找仍会越过该槽位继续探测；当流表完全为空时，再统一恢复为`EMPTY`。

主要理由：

- 长时间运行时，已结束连接不能永久占用流表；
- `last_seen`同时适用于离线PCAP时间和实时捕获时间；
- 截止时间由上层策略计算，流表只负责判断和移除；
- 删除标记既能释放业务容量，又不会破坏哈希冲突形成的探测链。

后续问题：

当前接口只完成底层删除机制。接入实时主流程时，过期记录正式移除前还需要先输出或交给下游，并由上层决定空闲超时时间和扫描周期，避免统计结果静默丢失。

## 8. 并发、输出与异常检测

### TD-013：单线程正确后再接pthread流水线

状态：实验完成，正式项目尚未接入。

决定：

正式链路先保持单线程；性能和接口稳定后，采用固定角色线程与有界阻塞队列连接采集、解析、流聚合和输出。

主要理由：

- 单线程更容易验证协议字段和所有权；
- 线程不能修复算法问题，过早并发会增加竞态、关闭顺序和调试成本；
- 项目已经通过`labs/thread_pipeline`验证生产者、处理者、输出者、条件变量和优雅退出；
- 固定流水线角色比通用线程池更符合当前数据处理顺序。

复审条件：

接入前先用性能数据确认瓶颈。如果抓包速度、解析速度和输出速度差异明显，再确定线程数量、队列容量和背压策略。

### TD-014：先结构化输出，再做Qt和云端

状态：计划采用。

决定：

核心程序先增加CSV或JSON输出，Qt上位机和可选云端都消费同一份稳定数据接口。Qt优先用于求职展示和本地调试，云端只在需要远程监控时增加。

主要理由：

- GUI不应该直接读取核心内部结构；
- 稳定的结构化输出可以同时服务测试、Python、Qt和远端上传；
- Qt适合展示实时流列表、协议分布和告警；
- 云端增加认证、网络中断、部署和运维成本，不应阻塞核心分析功能。

### TD-015：核心阶段不引入数据库

状态：暂缓。

决定：

当前核心只处理内存流表和文件或标准输出，不把数据库作为必需依赖。

主要理由：

- 当前目标是验证采集、解析、聚合和异常检测；
- 实时活动流适合放在内存中；
- CSV或JSON足以完成测试、演示和离线分析；
- 数据库会引入表结构、事务、写入性能和部署配置。

复审条件：

如果出现历史流查询、长期趋势、断电后保留、告警审计或多进程共享需求，可以先评估SQLite；如果数据规模和并发继续扩大，再评估独立时序或分析数据库。

### TD-016：规则检测优先于机器学习

状态：计划采用。

决定：

先基于包速率、字节速率、连接频率、端口扫描和协议状态实现可解释规则，再把稳定流特征交给Python训练模型。

主要理由：

- 规则容易验证和解释；
- 机器学习依赖数据集、标签、特征和评估指标；
- 先建立可靠的数据采集和特征链，才能判断模型效果；
- 开发板NPU可以作为未来扩展，但不是当前硬件选型或第一版算法的必要条件。

## 9. 硬件与部署环境

### TD-017：目标开发板选择LubanCat-2N

状态：已选型，等待到货后核验版本和实际配置。

决定：

目标板选择野火LubanCat-2N系列，具体V1、V2或V3以及内存和eMMC容量在到货后根据板背面丝印和系统信息补充。

与项目匹配的硬件特点：

- RK3568，四核64位Cortex-A55，最高2GHz，属于典型ARM64嵌入式Linux平台；
- 官方资料列出四网口设计，当前2N与2N-V2资源表为两个千兆口加两个2.5G口；
- 多网口适合模拟管理口、采集口、入口和出口，也适合后续软路由或旁路分析场景；
- LPDDR4／LPDDR4X和eMMC多容量可选，足以运行命令行Linux、libpcap和测试程序；
- 提供Debug串口、USB、TF卡、Mini-PCIe、SATA等接口，便于恢复系统、扩展网卡或保存PCAP；
- 官方提供OpenWrt、Linux镜像、设备树、SDK、原理图和源码资料，适合从应用层继续学习BSP和驱动边界；
- Cortex-A55和多网口比普通单网口开发板更贴合网络协议分析项目。

为什么没有只选择普通单网口板：

单网口板也能读取离线PCAP，但真实网关、双臂转发和多接口抓包需要额外USB网卡或交换设备。LubanCat-2N的网络接口更接近项目未来场景。

为什么没有为了NPU选择更昂贵平台：

当前瓶颈首先是采集、解析、流表和输出，不是模型推理。RK3568性能足以完成应用开发验证；板载NPU只作为未来可能的轻量模型扩展，不作为承诺。

风险与验证项：

- 标称网口速率不代表libpcap应用能够无丢包处理相同吞吐量；
- 需要验证四个网口对应的驱动、接口名称、链路速率和时间戳行为；
- GRO、LRO、校验和卸载等网卡特性可能改变抓包观察结果，需要记录开关状态；
- 持续高负载时需要观察CPU温度、降频和风扇需求；
- 不同硬件版本的设备树和接口可能不同，到货后必须按丝印选择正确镜像；
- 首次上板不直接承诺2.5Gbps线速分析，以实测包长、包率、CPU占用和丢包率为准。

到货后补充：

```text
板卡丝印：待填写
硬件版本：待填写
内存容量：待填写
eMMC容量：待填写
系统镜像：待填写
内核版本：待填写
四个网口驱动与接口名：待填写
```

参考：[LubanCat-2系列官方硬件介绍](https://doc.embedfire.com/linux/rk356x/quick_start/zh/latest/quick_start/lubancat/lubancat2.html)。

### TD-018：目标系统和首次部署方式

状态：计划采用，等待硬件。

决定：

- 首选厂商当前维护的64位命令行Linux镜像；
- 当前候选为Linux 6.1与Debian 12 lite，最终以到货时对应硬件版本的官方镜像为准；
- 第一次部署先在板端原生安装GCC、CMake、pkg-config和libpcap开发包并编译；
- 原生构建和测试稳定后，再增加ARM64交叉编译工具链；
- OpenWrt保留给后续软路由或网关部署实验，不作为第一次应用验证环境。

为什么先原生编译：

- 可以快速区分“源码不兼容”和“交叉工具链配置错误”；
- CMake和pkg-config直接使用目标系统真实依赖；
- 当前项目规模较小，原生编译时间可接受；
- 成功后再交叉编译，能够用相同测试结果做对照。

为什么候选Debian 12 lite：

厂商当前Linux 6.1镜像更新记录明确列出Debian 12、RK3568和LubanCat-2N支持；lite镜像没有桌面环境，更接近嵌入式服务部署，也能减少存储与后台资源占用。

参考：[LubanCat Linux 6.1镜像更新记录](https://doc.embedfire.com/linux/rk356x/quick_start/zh/latest/quick_start/baidu_cloud/update_history_rk356x_kernel6.1.html)、[系统镜像烧录说明](https://doc.embedfire.com/linux/rk356x/quick_start/zh/latest/quick_start/flash_img/flash_img.html)。

## 10. 版本管理与交付

### TD-019：Git功能分支、Pull Request和版本标签

状态：已采用。

决定：

- 每个相对独立功能使用单独提交；
- 团队流程通过功能分支和Pull Request审查后合并到`main`；
- 构建目录、可执行文件、大型PCAP、密钥和本机配置不提交；
- 小型、确定性、脱敏的测试输入可以进入测试目录；
- 达到明确验收条件后创建带说明的版本标签。

主要理由：

- 提交历史能够对应需求、实现和测试；
- Pull Request提供代码差异、自动测试和评审记录；
- 版本标签标记可重复构建的里程碑；
- 排除生成物和敏感数据可以保持仓库清晰安全。

## 11. 后续决策复审清单

以下问题满足条件后再更新本文档：

- 获得真实流量和性能数据后：记录负载因子、平均探测长度以及是否需要重建或动态扩容；
- 实时抓包完成后：记录权限模型、BPF过滤和丢包统计；
- 多线程接入后：记录线程数量、队列容量、所有权和关闭顺序；
- 结构化输出完成后：确定CSV、JSON或二者并存；
- Qt或云端开始后：确定进程间／网络接口和协议；
- 数据持久化出现真实需求后：重新评估SQLite或外部数据库；
- 开发板到货后：补充硬件版本、系统镜像、网卡驱动和性能数据；
- 异常检测完成后：记录规则阈值来源、数据集、特征和评估指标；
- 如果FNV-1a冲突或攻击风险成为实际问题：评估SipHash或其他带种子算法。

## 12. 面试表达原则

介绍技术选型时按照以下顺序说明：

```text
业务场景
  → 当前约束
  → 选择的方案
  → 没选其他方案的原因
  → 当前方案的代价
  → 测试或数据如何验证
```

例如FNV-1a的完整表达不是“因为它快”，而是：五元组只有13个业务字节，需要低开销、无动态内存、跨x86与ARM稳定的非加密哈希；FNV-1a实现短小且有固定参考流程，因此适合作为第一版。代价是不能防御恶意碰撞，所以哈希命中后仍比较完整流键，并保留未来改用带随机种子算法的条件。
