# 更新日志

本文件记录各个可发布版本的重要变化。版本号遵循语义化版本规则。

## [Unreleased]

### 新增

- 采集模块新增`capture_break_loop()`，在不关闭句柄的情况下请求libpcap中断阻塞读取；
- 应用上下文使用`volatile sig_atomic_t`保存停止请求，并在读取前后检查停止状态；
- 实时模式通过`sigaction()`接管`SIGINT`和`SIGTERM`，不启用`SA_RESTART`，退出前恢复原信号处理方式；
- 停止请求先中断活动采集，再沿正常控制流关闭句柄、输出包统计和流汇总；
- 在无匹配流量、已有部分ICMP流量和外部`SIGTERM`三种场景下完成人工验收，并通过全部15项CTest回归。

## [0.2.0] - 2026-08-26

新增实时抓包、BPF过滤和结构化流记录导出的版本。

### 新增

- 为定长流表增加按最后活动时间清理过期流的接口；
- 过期记录使用删除标记保留哈希探测链，并允许后续新流复用释放的槽位；
- 增加截止时间边界、活动流保留、容量复用和无效参数测试；
- 为规范化双向五元组增加跨平台稳定的FNV-1a 64位哈希计算；
- 哈希过程按固定字节序逐字段处理，避免结构体填充和主机字节序影响；
- 增加双向一致性、固定哈希向量、字段参与性和错误参数测试；
- 将定长线性流表升级为固定容量开放寻址哈希流表；
- 使用`EMPTY`、`OCCUPIED`和`DELETED`三种槽位状态，并以线性探测处理哈希冲突和数组回绕；
- 增加哈希冲突、回绕查找、删除标记连续探测和槽位复用测试；
- 新增独立的CSV流记录导出模块，输出协议、端点、双向统计和首末时间戳；
- CSV导出接口借用调用者提供的`FILE *`，由调用者负责文件打开、刷新和关闭；
- 增加CSV表头、完整记录格式和无效参数单元测试，CTest测试总数增加到15项；
- 命令行增加`--csv FILE`选项，在离线聚合成功后导出全部流记录；
- CSV文件使用C11独占创建模式，目标已存在时返回错误并保留原文件；
- 端到端测试验证CLI参数、PCAP分析、流表聚合和CSV落盘内容；
- 新增技术选型与架构决策记录，集中说明软件、算法、测试、环境和LubanCat-2N硬件选择；
- 采集模块新增实时网卡打开接口，支持配置快照长度、混杂模式和读取超时；
- 离线文件与实时网卡复用统一的采集句柄、链路类型查询、逐包读取和关闭接口；
- 增加实时抓包无效参数与不存在网卡测试，测试不依赖root权限或真实流量；
- CLI新增`--interface NAME --count PACKETS`实时分析模式，并严格验证正整数范围和参数组合；
- 实时与离线输入复用Ethernet、IPv4、TCP/UDP/ICMP解析、双向流聚合和汇总输出链路；
- 实时读取超时按暂时无数据处理，达到指定捕获包数后统一关闭句柄并输出结果；
- 在`lo`上完成真实TCP流量手工验收，并通过全部15项CTest回归测试；
- 采集模块新增`capture_set_filter()`，封装BPF表达式编译、安装、错误复制和临时程序释放；
- CLI新增仅用于实时模式的`--filter EXPRESSION`选项，并拒绝缺失、空、重复和离线组合；
- BPF在链路类型验证后、流表初始化和数据包读取前安装，`--count`只统计匹配后返回应用的数据包；
- 使用离线PCAP验证合法、非法、匹配和排除语义，并在`lo`上通过ICMP过滤人工验收及全部15项CTest回归。

## [0.1.0] - 2026-08-24

首个能够完成离线PCAP分析的可用版本。

### 新增

- 基于libpcap读取离线Ethernet PCAP文件；
- 使用边界检查游标安全读取网络字节；
- 解析Ethernet II、IPv4、TCP、UDP和ICMP头部；
- 统一记录数据包时间戳、捕获长度、线上长度和解析状态；
- 按IPv4协议号分发传输层解析器；
- 使用规范化双向五元组，将两个方向的数据包聚合到同一条流记录；
- 分方向统计包数、字节数以及首末数据包时间戳；
- 使用调用者提供存储的定长流表完成离线流量聚合；
- 命令行支持`--read`和`-r`读取PCAP文件；
- 完整分析全部数据包，同时只预览前5个包，并输出流汇总；
- 13个C语言单元测试和1个Python端到端验收测试；
- 目标环境只读检查脚本，为后续ARM Linux上板验证做准备。

### 修复

- 修复逐包预览计数未递增导致序号和显示上限错误的问题；
- 将预览数量与分析数量分离，避免只聚合前5个数据包；
- 修复离线流量验收脚本中的Python缩进错误。

## [0.0.1]

### 新增

- 建立Linux C11、CMake和CTest工程骨架；
- 建立命令行入口、应用上下文和核心静态库；
- 支持帮助、版本和未知参数处理；
- 导出`compile_commands.json`并启用严格编译警告；
- 添加主程序冒烟测试。
